%!TEX root=../main.tex
\chapter{Discussion \& Limitation}%todo change title to match contents
\label{chap:discussion}
   - What worked, what didn't, why
   - Generalization, runtime, practical constraints
   - Integration considerations for IRT pipeline


   \section{Problems}%find appropriate title
   for small datasets, it could happen that the val dataset gave better results than the train dataset consistently throughout training. this is unexpected. (i think becasue of uneven class distribution. if not resolved for big datasets, see if i messed up the split. should also improve when i make it class aware) THis was resolved by introducing data .. (das was tim wollte) to ensure even distribution with regards to ...



 so argumentieren: vergleich einfache lstm und chronos. dann etwas feststellen (wie rum eigentlich egal hierfür). limitations von chronos hinweisen mit kritik link aus slack an transformern. sagen xlstm lässt foundation models zu, die dieses problem nicht haben. warte auf diese modelle und hier anwenden-> future work